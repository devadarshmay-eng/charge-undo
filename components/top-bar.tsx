"use client";
import type { Station } from "./station-types";
type Props = { stations: Station[]; query: string; onQuery: (query: string) => void; filters: Set<string>; onFilter: (filter: string) => void; onSelect: (station: Station) => void };
const filters = [["available", "Available"], ["occupied", "In use"], ["unconfirmed", "Unconfirmed"], ["disputed", "Disputed"]];
export function TopBar({ stations, query, onQuery, filters: active, onFilter, onSelect }: Props) {
  const results = query ? stations.filter((s) => `${s.name} ${s.address} ${s.operator ?? ""} ${s.connectorType ?? ""}`.toLowerCase().includes(query.toLowerCase())).slice(0, 7) : [];
  return <header id="topbar"><div className="row1"><div id="searchwrap" className={query ? "has-q" : ""}><div id="searchbox" className="srf"><span>⌕</span><input id="search-input" value={query} onChange={(event) => onQuery(event.target.value)} placeholder="Search stations" /><button id="clear-search" onClick={() => onQuery("")}>×</button></div>{query && <div id="search-drop" className="srf open">{results.length ? results.map((station) => <button className="sd-row" key={station.id} onClick={() => onSelect(station)}><span className="st-ic"><i className="sdot" /></span><span className="sd-main"><b className="sd-name">{station.name}</b><small className="sd-sub">{station.operator ?? station.address}</small></span><span className="sd-side">{station.connectorType ?? ""}</span></button>) : <div className="sd-empty"><b>No matches for “{query}”</b>Try an operator or neighborhood.</div>}</div>}</div><button className="icobtn srf" aria-label="Saved stations">♡</button></div><nav id="chipbar">{filters.map(([key, label]) => <button className={`chip ${active.has(key) ? "on" : ""}`} key={key} onClick={() => onFilter(key)}><i className="cdot" />{label}</button>)}</nav></header>;
}
